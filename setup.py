from skbuild import setup

setup(
        name = "flynet-optimizer",
        version = "0.1.0",
        description = "optimizer for flynet",
        author = "Johan Melis",
        license = "MIT",
        python_requires = ">=3.8",
        packages=["flynet_optimizer"],
        )
